import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.masertransportes.appmaser',
  appName: 'Maser',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
